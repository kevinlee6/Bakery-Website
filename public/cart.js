const add = document.getElementsByClassName('add');

const template =
    `<div>
        
    </div>`;

for (let i of add) {
    i.addEventListener('click', e => {
        e.preventDefault();


    })
}